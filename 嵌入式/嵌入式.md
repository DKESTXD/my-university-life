综合题包括：系统框图，流程图，程序

可以带尺子？带计算器

## 1 数电部分

### 1.1 数码制

数字量：变化在时间上和数量上都是不连续的，存在一个最小数量单位△

模拟量：数字量以外的物理量

2与16进制转化，每四位为一组，从右向左组合，不够的补0

二进制的加减乘除都可以用移位来解决：

二进制加法：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\eb6344f20e1830068a4f1c3ee41650b.png" alt="eb6344f20e1830068a4f1c3ee41650b" style="zoom:50%;" />

二进制减法：减去一个数相当于加上他的补码

二进制乘法：乘2就是左移1位，乘4就是左移2位，除法原理相同。

补码：符号位0+1-，除符号位都取反+1

### 1.2  逻辑运算

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\1d9d371971d75a478b7d152844c1e42.png" alt="1d9d371971d75a478b7d152844c1e42" style="zoom: 33%;" /><img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\53171bbd09018aae3cec4fc7b3b52fe.png" alt="53171bbd09018aae3cec4fc7b3b52fe" style="zoom: 33%;" /><img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\49759a0116e0f2dcbe2d973abc40241.png" alt="49759a0116e0f2dcbe2d973abc40241" style="zoom: 33%;" />

图案也记住

与非  或非 记一下（与可以用与非+非来实现）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\9c4e9011be0da69f90f7b4add7836a8.png" alt="9c4e9011be0da69f90f7b4add7836a8" style="zoom:50%;" />

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\67db7c183bb178fafc647ef3f1f4c0c.png" alt="67db7c183bb178fafc647ef3f1f4c0c" style="zoom: 50%;" />

异或是加法基础

门电路：用半导体器件可以构建有逻辑功能的电路

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\a5acf333651223449e7262e15d313cf.png" alt="a5acf333651223449e7262e15d313cf" style="zoom: 67%;" />

会判断就行

有的芯片是多个与非门的集成，组合多个管脚

### 1.3 组合逻辑电路

#### 1.3.1 概念

数字电路是由简单的门构成复杂的功能，组合电路由**门电路组成，不含记忆（存储）元件**；**其输出只与该时刻的输入有关**。

时序逻辑电路**包含记忆元件**，**其输出不仅取决于当前时刻输入，也和电路原来的状态有关**

非时序逻辑电路就是组合逻辑电路

#### 1.3.2 举例

只要会举例，能说出名字，知道大概实现什么功能，具体结构与功能不用管

编码器：把一堆输入转化为二进制编码

译码器：把一堆二进制编码转化为输出

八段数码管（必考）：共阴：亮对应1对应高电平；共阳：亮对应0对应低电平

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\4e2444f32363b6d242ca4f48c923a12.png" alt="4e2444f32363b6d242ca4f48c923a12" style="zoom:60%;" />

按照a-h，从右到左编码

加法器：1位加法器（半加器），全加器，只需知道用一堆与非逻辑门可以搭出来一个加法器即可

串行加法器：按照从低到高一位一位加上去，结构简单但是慢

并行加法器：第i位的进位是输入信号两个加数第i位之前各位(0~j-1)的函数，可以在相加前由AB两数确定。结构复杂但是快，每一位都基本同时产生。

（不用记住结构，只知道有这么两个东西存在即可）

### 1.4 时序逻辑电路

记忆单元：触发器：一个逻辑门的输出是另一个的输入

（记住一个就行，没怎么考过）

D触发器：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\5a29626c6ad7b3fdfe0fdfe2419c394.png" alt="5a29626c6ad7b3fdfe0fdfe2419c394" style="zoom:50%;" />

Q是下一时刻的输出，Q*是上一时刻输出

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\013bae10cffe185ff5b80ede91ed739.png" alt="013bae10cffe185ff5b80ede91ed739" style="zoom:50%;" />

如果是简答题考到就画这张图

#### 1.4.1 举例

寄存器：寄存一组二值代码

移位寄存器：存储加移位

同步/异步计数器：计数的

只要能说出名字，知道是干什么的，无需记结构

## 2  8051部分

冯诺依曼体系结构

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\c253a50752f78b33575ed68561f300c.png" alt="c253a50752f78b33575ed68561f300c" style="zoom:50%;" />

可能根据具体问题，问你这几个模块怎么工作的。

### 2.1 汇编Assembly language

只考判断选择

由什么构成：

\[label\]:Mnemonic \[operands\] \[;comment\]

标号：助记符  操作数  ;注释

MOV移动；ADD相加；SJMP跳转

### 2.2 单片机CPU三总线结构

bus： The CPU is connected to memory and I/O through  strips of wire called a bus. Carries information from place to place

有三条总线：Address bus， Data bus， Control bus

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\67c6bf692bc57a755120d10efa9c29b.png" alt="67c6bf692bc57a755120d10efa9c29b" style="zoom: 67%;" />

### 2.3 PSW

ALU：完成算术运算、逻辑、位运算等等，相关寄存器有A B PSW

A(ACC)： For all arithmetic and logic instructions（算术与逻辑）

B：For multiplication and division（乘除法）

PSW：also referred to as the flag register, is an 8 bit  register.Only 6 bits are used（状态寄存器）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f799b742f9746e811e907695d873932.png" alt="f799b742f9746e811e907695d873932" style="zoom:60%;" />

每一位分别是什么

### 2.4 Stack堆栈

只考选择题

8051中堆栈指针指向最后一个被用掉的值，当push的时候先指针加一再写入数据；当pop的时候先将数据读出去，再写入数据

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\76fe1bba910c54c505668a3e5174896.png" alt="76fe1bba910c54c505668a3e5174896" style="zoom:60%;" />

### 2.5 PC 

PC (program counter) points to the address of the  next instruction to be executed（指向下一条被执行指令的位置）

当遇到中断响应时，将当前PC值压入堆栈，PC改到中断响应程序处，执行完后PC值出栈继续运行

### 2.5 register

Register are used to store information temporarily,  while the information could be: a byte of data to be processed, or  an address pointing to the data to be fetched（数据或者是指向可获取的数据的地址）

The vast majority of 8051 register are 8-bit registers There is only one data type, 8 bits, any data larger than  8 bits must be broken into 8-bit chunks before it is  processed（大部分是八位，多余八位的要分为八位块）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\6d7ffcfe8c35db058ef725b70bb9d7b.png" alt="6d7ffcfe8c35db058ef725b70bb9d7b" style="zoom:60%;" />

右边是低位，左边是高位

Internal RAM（选择题概率高）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\db426dc498842b6c88df4c0d878c8cc.png" alt="db426dc498842b6c88df4c0d878c8cc" style="zoom:60%;" />

0x00-0x1F：工作寄存器区

0x20-0x2F：位寄存器，有单独编号，不会考这么细

0x30-0x7F：用户可直接用的RAM

0x80-0xFF：特殊功能寄存器SFR，Direct Addressing used to access  SFRs

具体的一些汇编指令**应该不考**：MOV移动，DJNZ条件跳转，LJMP长跳转，SJMP短跳转，LCALL，ACALL（应该和地址有关吧？）  

MOV的操作数**#十六进制**表示数据写入，**寄存器**表示数据加载

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\746b64edf182713d7938e58b1d5c557.png" alt="746b64edf182713d7938e58b1d5c557" style="zoom:60%;" />

### 2.6 Timing（重要）

都是12兆晶振， the  frequency of the crystal oscillator 晶振频率

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\1dda9de4e8e511081289071939c6fa8.png" alt="1dda9de4e8e511081289071939c6fa8" style="zoom:60%;" />

oscillator periods振荡周期：1/12 μs

machine cycle机器周期：1μs

指令周期：单周期指令；双周期指令；四周期指令

In original 8051, one machine cycle lasts 12  oscillator periods

CPU executing an instruction takes a certain number of clock cycles .These are referred as to as machine cycles(机器周期定义)

### 2.7 I/O Ports Configuration管脚

一共40个

P3系列比较重要，P3有8位各位都有additional function第二功能，一个功能占2位，共有四个功能。

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\3bef6430ebac2a91c76b78c9118dcf1.png" alt="3bef6430ebac2a91c76b78c9118dcf1" style="zoom:60%;" />

自上而下为串行通讯，外部中断，外部计数器，读写功能

### 2.8 else

位寻址I/O Ports and Bit Addressability，**不需要记**

unsigned number的加法

带进位的加法

减法，乘法（知道乘法比较慢），除法

**都不需要记，因为都是讲的汇编指令的使用方法**

### 2.9 C programming

unsigned char 8位最常见

sbit定义特殊功能位（管脚）

sbit a=P2^4；

代码基本框架

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f838cd8050afdf3036e5cea7ac662e5.png" alt="f838cd8050afdf3036e5cea7ac662e5" style="zoom:60%;" />

毫秒延迟的函数框架：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\79ab18a9f8c98e7b127e2e28b959264.png" alt="79ab18a9f8c98e7b127e2e28b959264" style="zoom:60%;" />

这里的1275是针对于11.0592MHZ晶振算的，如果用12MHZ晶振，12/11.0592*1275=1383.46，可以取1383。

控制管脚高低电平直接令定义的管脚对应的变量为1或0

读位可寻址RAM，用bit，也就是位寄存器区域的数据（不重要）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\d794366f8e7c7897875d0e5f72d215b.png" alt="d794366f8e7c7897875d0e5f72d215b" style="zoom:60%;" />

c语言的与或非，位运算（加上右移>>和左移<<）

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\991927ea45f609d3286bd01c74bb8c4.png" alt="991927ea45f609d3286bd01c74bb8c4" style="zoom:60%;" />

ascii码和数字转换，首先将其每位分开，（120：120/100，120/10%10，120%10）

通过串口发送需要对每一位转为ascii码：1+'0'   或者  1+0x30  或者 1 | 0x30

### 2.10  Interrupt中断

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\fc5a3367f18ecbc41a354a91571fff9.png" alt="fc5a3367f18ecbc41a354a91571fff9" style="zoom:67%;" />

查询和中断

RETI是中断返回

#### 2.10.1 中断的步骤Steps in Executing an Interrup



1. It finishes the instruction it is executing and saves the  address of the next instruction (PC) on the stack   PC保留堆栈
2.  It also saves the current status of all the interrupts  internally (i.e: not on the stack)     其他状态值保存
3. It jumps to a fixed location in memory,  called the  interrupt vector table, that holds the address of the ISR    跳转到固定内存位置，被称为中断向量表，包含ISR的地址
4. The microcontroller gets the address of the ISR from the  interrupt vector table and jumps to it. It starts to execute the interrupt service subroutine until it reaches  the last instruction of the subroutine which is RETI     跳转到固定入口地址执行中断响应程序
5. Upon executing the RETI instruction, the microcontroller  returns to the place where it was interrupted  执行RETI返回中断的位置
   - It gets the program counter (PC) address from the stack bypopping the top two bytes of the stack into the PC   通过从堆栈中弹出前两个字节来获得PC   Then it starts to execute from that address

#### 2.10.2  interrupt system structure chart 

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f7b8c1373dfe9e4ab6b5b9d443fc90a.png" alt="f7b8c1373dfe9e4ab6b5b9d443fc90a" style="zoom:60%;" />

中断所有关键字：

中断源：外部中断0-定时器0；外部中断1-定时器1；串口

中断标志：IE0   TF0   IE1   TF1   RI   TI

中断开关IE：EX。。。   总开关EA

中断优先级

执行顺序默认是从地址地位向高位，即自上而下，写成二进制就是从右向左

#### 2.10.3 IE interrupt  enable  中断开关

具体对应关系参考上面的chart 

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\44d0f3c63b16aa3ea061b778557ce85.png" alt="44d0f3c63b16aa3ea061b778557ce85" style="zoom:60%;" />

 ET2  ：Enables or disables timer 2 overflow or capture  interrupt (8052）

EA=1:中断使能打开

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\0a241bd62398460fa5b0c5e6f884323.png" alt="0a241bd62398460fa5b0c5e6f884323" style="zoom:50%;" />

#### 2.10.4 中断向量表Interrupt vector table  不重要

表示了各个中断跳转的地址

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\e3f78fb996de494fa8300058b6a8b4f.png" alt="e3f78fb996de494fa8300058b6a8b4f" style="zoom:67%;" />

#### 2.10.5 IP interrupt  priority  中断优先级

 对应关系也参考上面的chart

interrupt  priority  register  中断优先级寄存器  bit-addressable

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\1e18e0713c6f4e7352ce2f019b5688a.png" alt="1e18e0713c6f4e7352ce2f019b5688a" style="zoom:67%;" />

PT2    Timer 2 interrupt priority bit (8052 only)

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\14b649c7ffab31ed6799fc59538fed0.png" alt="14b649c7ffab31ed6799fc59538fed0" style="zoom:60%;" />

**例子：**

IE：10001101

开了三个中断依次为：外部中断0，外部中断1，定时器1

IP：00001010

首先看优先级的对应关系，按照地址从小到大的顺序，即从右向左，第一个有优先级的对应定时器0，但是他没开，第二个有优先级的是定时器1，因此首先执行定时器1；然后回看IE，依旧按照地址从小到大的顺序，先执行外部中断0，再外部中断1  。那么最终的顺序为定时器1  外部中断0  外部中断1

#### 2.10.6  c语言的中断响应

c语言中定义中断响应时，要在定义的函数后面加上关键字

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\3d242042880860f1d3bea91d6cbb96e.png" alt="3d242042880860f1d3bea91d6cbb96e" style="zoom: 50%;" />

### 2.11 timer 定时器

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f2818b92f39b4ce3b4a91ccf4d8bb12.png" alt="f2818b92f39b4ce3b4a91ccf4d8bb12" style="zoom:60%;" />

GATE0不考；TR0控制开关是否开，取0时不计数；计数来源可以是十二兆晶振，记一次1μs，也可以来自于T0计数；在plus1 counter处进行计数，是16位计数，TH0是高八位，TL0是低八位，当其计数到FFFF后TF0等于1，计时结束。

算TH0  TL0，对12MHZ晶振计一次为1μs，十六位最大为65536，假设计时3ms即3000μs，65536-3000=62536，再转换为16进制分配给TH0  TL0。相当于初值的计算。

设置字节不考

定时器只用模式1或2

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\4ae01834efdef440ed7a7fb81d20f9a.png" alt="4ae01834efdef440ed7a7fb81d20f9a" style="zoom:67%;" />

模式1就是前面讲的：步骤为：（不是很重要）

1. Load the TMOD value register indicating which timer  (timer 0 or timer 1) is to be used and which timer mode (0  or 1) is selected   先加载TMOD
2.  Load registers TL and TH with initial count value  加载TH0  TL0
3.  Start the timer
4. Keep monitoring the timer flag (TF) with the JNB TFx,  target instruction to see if it is raised.Get out of the loop when TF becomes high   持续监控TF    直到TF溢出
5. Stop the timer  停止定时器
6.  Clear the TF flag for the next round  清除TF标志
7.  Go back to Step 2 to load TH and TL again  回到step2 重置



模式2 使用的是八位计数

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\293faa35cb4d048a0bb1311be4cda19.png" alt="293faa35cb4d048a0bb1311be4cda19" style="zoom:60%;" />

 

在TH中存储初值，溢出后值给TL，当TL在256时溢出，TF=1；然后TF清除标志，初始值重新回到TH。

相较于模式1，在step4处

 When the TL register rolls from FFH to 0 and TF is set to  1, TL is reloaded automatically with the original value kept  by the TH register

To repeat the process, we must simply clear TF and let it go without  any need by the programmer to reload the original value

这使得模式2可以自动重新加载



TCON  不用全记住

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\fce4915335718cef6e3562cefc55a2e.png" alt="fce4915335718cef6e3562cefc55a2e" style="zoom:60%;" />

只需要记住TF1 TR1  TF0  TR0 

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\fc0275d891bf83fc327455696d28083.png" alt="fc0275d891bf83fc327455696d28083" style="zoom:60%;" />

典型例子，这里的TH0与TL0都是11.0592MHZ求的，一般用12MHZ 。

对右侧定时器每行的注释是：

- 初始化为模式1
- 加载初值
- TR0=1 启动定时器
- while 等待定时结束
- TR0=0 停止定时器
- TF0=1 清除定时标志

和上面的工作流程是完全对应的

### 2.12 serial communication  串口通讯

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\1b158a491439587bd826e922a11f1b0.png" alt="1b158a491439587bd826e922a11f1b0" style="zoom:60%;" />

这个图不考，串行通讯是一位一位发出的，平时是高电平，首个低电平标志开始，是起始位。发送的时候先低位后高位。

为什么要有波特率：To allow data transfer between the PC and an 8051 system  without any error, we must make sure that the baud rate of  8051 system matches the baud rate of the PC’s COM port  双方要有相同的频率来解析发送的波

当进行串口通讯时，为了解析不同值，所用的定时器的TH1也相应的不同，以MOD2工作时，baud rate与TH1的对照为,背下来就行

9600~ -3  	FD

19200 ~ -3       FD

57600 ~ -1      最高频率溢出，初值为FF 

28800 ~ -2    



SBUF register

发送和接收的缓存，但是有两个不同的缓存器

SCON 不需要记

SCON的RI  TI 是中断标志

串行通讯工作过程：

发送数据

1.  TMOD register is loaded with the value 20H, indicating the use of timer1   in mode 2 (8-bit auto-reload) to set baud rate  设置模式2自动可重载
2. The TH1 is loaded with one of the values to set baud rate for serial data  transfer    算波特率
3. The SCON register is loaded with the value 50H, indicating serial mode1,  where an 8-bit data is framed with start and stop bits   设置串行通讯模式中断
4. TR1 is set to 1 to start timer   开定时器，启动波特率发生器
5. TI is cleared by CLR TI instruction  
6.  The character byte to be transferred serially is written into SBUF register
7.  The TI flag bit is monitored with the use of instruction JNB TI , xx to see if  the character has been transferred completely
8.  To transfer the next byte, go to step 5

TI代表发送一个数，先清零，发送一个数，发送完成后TI变成1，可以进行下一次发送（一个用来标记发送成功的flag）

RI表示接收一个数，接收成功RI=1，但是RI什么时候能够触发取决于发送端（抽象判断题）

接收数据：

1. TMOD register is loaded with the value 20H, indicating the use of  timer 1 in mode2 (8-bit auto-reload) to set baud rate
2. TH1 is loaded to set baud rate
3. The SCON register is loaded with the value 50H, indicating serial  mode 1, where an 8-bit data is framed with start and stop bits
4. TR1 is set to 1 to start timer 1
5. RI is cleared by CLR RI instruction
6. The RI flag bit is monitored with the use of instruction JNB RI,xx to  see if an entire character has been received yet
7. When RI is raised, SBUF has the byte, its contents are moved into a  safe place
8.  To receive the next character, go to step 5



波特率加倍：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\ecbe9d2854d107088a21f3adc6f25f2.png" alt="ecbe9d2854d107088a21f3adc6f25f2" style="zoom:60%;" />

有一个SMOD可以标定波特率是否加倍（不重要），比如9600不加倍是-3，加倍是-6 对于TH1来说

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\e67695d4ab5fa567eb90d7fb999a0ce.png" alt="e67695d4ab5fa567eb90d7fb999a0ce" style="zoom:67%;" />

这个程序展示了串行通讯发送过程，其流程和上述讲过的一致。TR1表示打开定时器，TI是发送成功的flag

这里SBUF='A'，表示串行通讯发送一个A

如果是ACC=SBUF，表示串行通讯接收值

### 2.13 interface

LCD   ADC   KEY

#### 2.13.1 矩阵按键

 按键识别方法：行扫描和线路反转法

行扫描：

设置行线为输出，列线为输入，当无按键按下时，列输入全 为“1”。设计时，将某一行输出为“0”，读取列线值， 若其中某一位为“0”，则表明行、列交叉点处的按键被按 下，否则无按键按下；继续扫描下一行（将下一行输出为 “0”），直至全扫描完为止。 
或同理可设置列线为输出，行线为输入。设计时，将某一列 输出为“0”。

线路反转法：需要两个双向I/O口分别接行、列线

1. 由行线输出全“0”，读入列线，判有无键按下（若有 一个列为“0”则表明有键按下）。
2. 若有键按下,再将读入的列值从列线输出,读取行线的值。 
3. 第一步读入的列值与第二步读入的行值运算， 从而得到代表此键的唯一的特征值。

判断速度快，两次即可

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\8e6779bd103336d4e44198c6bb63ce8.png" alt="8e6779bd103336d4e44198c6bb63ce8" style="zoom:60%;" />

#### 2.14.2 led显示

和前面一样

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\722fb0bfde1bd8c10c4aa635cfafbf5.png" alt="722fb0bfde1bd8c10c4aa635cfafbf5" style="zoom:60%;" />

#### 2.14.3 lcd1602

不考，复习个毛

## 3 嵌入式系统

### 3.1 定义

以应用为中心、以计算机技术为基础，软硬件可裁减，适用于应用系统的、对功能、可靠性、成 本、体积、功耗有严格要求的专用计算机系统。

组成模型：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\9b48d0d6efea9362190e178f43e0d66.png" alt="9b48d0d6efea9362190e178f43e0d66" style="zoom:60%;" />

冯诺依曼的体系结构只是嵌入式系统里面的某一个芯片的结构

### 3.2 cortex M3

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\1f87c4ed591cd1803b66d37f2c75e4d.png" alt="1f87c4ed591cd1803b66d37f2c75e4d" style="zoom:60%;" />

这个图记下来，最多简答题

相较于8051：有一个复杂的中断，有三级流水线，有debug系统，多了内存保护单元



三级流水线：取指令   译码    执行

三个步骤占用的资源都不一样，三者时间上是错开的

![9e4ad1816076de444ac7f9fc7e3962a](D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\9e4ad1816076de444ac7f9fc7e3962a.png)

正常操作过程中，在执行一条指令的同时 对下一条指令进行译码，并将第三条指令从存 储器中取出。

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f428a8276e8f8c390c3aaa833a9287c.png" alt="f428a8276e8f8c390c3aaa833a9287c" style="zoom:60%;" />

thumb-2  相较于原来的内核性能没有衰减，但是代码容量只有之前的3/4，效率更高

### 3.3 两种工作模式

1）线程模式和处理模式

目的：引入处理模式（handle mode）和线程模式 (thead mode) 的本意，是用于区别普通应用程序 （线程模式）和中断服务程序（处理模式）； 

效果：有效的简化了ARM7体系结构支持7种处理器 模式(用户模式、快中断模式、中断模式、管理模式、 中止模式、未定义模式和系统模式)。

2）特权级和用户级：

目的：用于存储器访问的保护机制。使得普通的用户程序代码 不能意外地，甚至是恶意地执行涉及到要害的操作；

 特权级：该级别的程序可以访问所有范围的存储器（如果有 MPU，还要在MPU 规定的禁地之外），并且可以执行所有指 令； 

用户级：用户级程序不能直接改写CONTROL 寄存器，需执行 一条系统调用指令(SVC)，由异常服务例程修改CONTROL 寄 存器，才能在用户级的线程模式下重新进入特权级。

3）默认状态：

在CM3 运行主程序（后台程序）时是线程模式， 既可以使用特权级，也可以使用用户级；

中断(异常)服务程序必须在处理模式（始终特 权级）下执行；

复位后，mcu默认进入线程模式，特权极访问。

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\8de09b599944f6c9afab6d35a0d582e.png" alt="8de09b599944f6c9afab6d35a0d582e" style="zoom:60%;" />

（看着这么多，其实最多考个选择题）

### 3.3  存储器保护单元MPU

stm32不允许随意改内存值，改sfr值，只能通过函数调用来改

### 3.4 内部寄存器

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\14343275ee6844039ae31cd6593f1a2.png" alt="14343275ee6844039ae31cd6593f1a2" style="zoom:60%;" />

记住下面三个，最多考个选择题

### 3.5 中断机制

中断优先级数目有八位 256级

中断可配置的数目有240个

stm为了加速中断有占先和末尾连锁

占先：优先级打断

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\4848107e9fece7cee59b9a7a7854cbd.png" alt="4848107e9fece7cee59b9a7a7854cbd" style="zoom:60%;" />

末尾连锁：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\9ec5dee587c4d663582077d5f8c001c.png" alt="9ec5dee587c4d663582077d5f8c001c" style="zoom:60%;" />

一个中断结束后不像8051后会到原位出栈再堆栈，而是直接跳转到另一个中断，最后一起出栈。

### 3.6 存储器

stm32 是32位的，因此

一个字节8位，一个字有32位，即4个字节。一个半字有16位

存储器格式从来不考

CM3处理器两种调试接口（没考过，但是也记一下）

JTAG: 6线制接口

 SWD: 2线制接口

### 3.7 功能部件

读代码

#### 3.7.1 GPIO

① DI   数字量输入：高电平1，低电平0，输入中断、唤醒等； 

② DO 数字量输出：高电平1，低电平0； 

③ AF  其它功能：引脚复用，预定义的功能模块引脚；

工作模式：

1）input floating ：浮空输入，引脚电平不确定

input pull-up：上拉输入，引脚高电平，输入低电平时起作用

input pull-down：下拉输入，引脚低电平，输入高电平起作用

2）output push-pull：推挽输出，输出非0即1

output open-drain：开漏输出，不附加上拉电阻时没有高电平输出能力

3） Alternate function push-pull、 open-drain：交替功能式输出

应该考不了这么细

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\d5ed563f76e7e580e2c3c0a2e015fec.png" alt="d5ed563f76e7e580e2c3c0a2e015fec" style="zoom:60%;" />

记住函数的读写用这两个函数，不会让你写，最多标注释

#### 3.7.2 定时器

数目更多：4个通用16位定时器

模式更多：上升、下降、上升和下降

T1 为增强型，带互补输出，紧急停止等功能，可用于电机控制

中断产生条件：定时器溢出、比较值相等、扑获引脚有 指定的跳变

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\f13158f7c841f80e8a080ac8dcf10b2.png" alt="f13158f7c841f80e8a080ac8dcf10b2" style="zoom:60%;" />

记住清楚中断标志，读中断状态的函数，

### 3.7.3 串行通讯usart

和8051一样，都是异步的，波特率也一样，只是更快一些

工作流程：

接收过程：UART监听总线，有下跳变时，启动 数据采样，一个字节的数据收到后，如果奇偶校 验正确，则把数据存到接收寄存器中，置状接收 态标志位，并向CPU申请中断，让CPU及时读取；

发送过程：UART的发送寄存器接收到CPU写入 的数据，立刻启动，按设定的参数逐位发送，发 送完毕，置状发送态标志位，并向CPU申请中断， 告诉CPU可以发送下一个字节。

和8051大致相同

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\0f4181b2b2708af2296f5b6ec5467c3.png" alt="0f4181b2b2708af2296f5b6ec5467c3" style="zoom:60%;" />

检查中断状态，接收串行通讯数据，发送串行通讯数据，判断是否发送完成

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\0d70d0fefed6c048abeb801785649a7.png" alt="0d70d0fefed6c048abeb801785649a7" style="zoom:60%;" />

具体怎么初始化的不用记，知道叫什么名字对应哪个模块就行

#### 3.7.4  A/D 必考

ADDA数值转换，线性关系：

对于普通的12位，最大数字值为4096 。 已知Vref=2.5V，Vin=0.75V，Vin/Vref=D数字值/4096

或者反之用数字值求电压值



读代码：

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\4566f6020139e94533995157a523164.png" alt="4566f6020139e94533995157a523164" style="zoom:60%;" />

AD软件触发启动，等待转换结束，得到转换的值。连续做50次转换求均值（难度接近考试？）

#### 3.7.5 D/A 必考

与AD类似，一般0-2.5V     也可能3.3V，看具体题目

<img src="D:\新建文件夹 (3)\WeChat Files\wxid_uzqin1xmai4h22\FileStorage\Temp\28d0a113e82dde9a0fa1475f1df54d9.png" alt="28d0a113e82dde9a0fa1475f1df54d9" style="zoom:60%;" />



可能会让你画输出的波形，如阶梯波

DA输出最大是4096



<img src="D:\新建文件夹 (5)\09e18f3258a2a42a29d123d8cfccea0.jpg" alt="09e18f3258a2a42a29d123d8cfccea0" style="zoom: 25%;" />